Run linting and formatting for this repository.

1. Run `bun run lint:fix` to auto-fix issues with `oxlint`.
2. Run `bun run format` to format files with `oxfmt`.
3. Report any remaining issues that could not be fixed automatically.
