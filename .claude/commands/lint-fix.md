Run linting and formatting on the codebase.

1. Run `bun run lint:fix` to auto-fix lint issues with oxlint
2. Run `bun run format` to format code with oxfmt
3. Report any remaining issues that need manual attention
